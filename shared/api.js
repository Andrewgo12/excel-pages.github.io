/* shared/api.js
   Converted from shared/api.ts — Type-only exports removed.
   The original file only declared a TypeScript interface (DemoResponse),
   which is not needed at runtime in JavaScript.
*/

export {}; // mantiene el módulo como módulo ES, no exporta tipos (solo para compatibilidad)
